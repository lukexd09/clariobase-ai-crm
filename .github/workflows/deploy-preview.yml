name: Retired Deploy Preview

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Reject retired workflow
        shell: bash
        run: |
          echo "Direct Deploy Preview is retired because it bypasses exact Fast CI correlation. Run Preview Release instead." >&2
          exit 1
