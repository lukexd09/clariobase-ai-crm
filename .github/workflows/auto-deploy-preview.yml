name: Retired Auto Deploy Preview

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Reject retired workflow
        shell: bash
        run: |
          echo "Auto Deploy Preview is retired. Run Preview Release with pr_number and optional expected_sha." >&2
          exit 1
